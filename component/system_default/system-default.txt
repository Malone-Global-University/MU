<!--Here’s a clear breakdown of extracted theme and components organized it for easy reference -->

<!-- 1. Theme -->

<! --Your theme is fully CSS-based with light, dark, and system modes. It’s controlled via CSS variables and a toggle script. -->

/* CSS Variables */
/* Dark theme default */
:root {
  --bg: #121212;
  --card: #1e1e1e;
  --text: #f0f0f0;
  --accent: #58a6ff;
  --accent-hover: #377bb5;
}

/* Light theme */
@media (prefers-color-scheme: light) {
  :root {
    --bg: #f9f9f9;
    --card: #ffffff;
    --text: #121212;
    --accent: #0055cc;
    --accent-hover: #003f99;
  }
}

/* Theme classes */
:root.light { ... }  /* Light theme override */
:root.dark { ... }   /* Dark theme override */

<*Theme Behavior*/

Default: system preference (prefers-color-scheme)

User can toggle: light, dark, system

Persisted using localStorage

Toggle icon updates dynamically:

Light: ☀️

Dark: 🌙

System: 💻

2. Components

Here are the main UI components and their structure:

Navbar

Class: .navbar

Fixed top with logo and nav links

Elements:

.logo-container → logo image + school name

.nav-links → list of navigation links

.hamburger → theme toggle button

Key styles:

.navbar { position: fixed; top: 0; width: 100%; background: var(--card); ... }
.nav-links { display: flex; gap: 1.5rem; margin-left: auto; }
.hamburger { background: var(--accent); color: #fff; ... }

Hero Section

Class: .hero

Full-width gradient background

Centered text with headline + description

Elements:

<div class="hero">
  <h1>Title</h1>
  <p>Description</p>
</div>


Key styles:

.hero { background: linear-gradient(...); text-align: center; color: #dbefff; }
.hero h1 { font-size: clamp(1.8rem, 5vw, 3rem); }
.hero p { font-size: clamp(1rem, 2.5vw, 1.25rem); }

Dictionary List

Class: .dictionary-list

Container for dictionary items

Each entry: .dictionary-item

<h2> for term

<a> for link to the entry

Key styles:

.dictionary-item {
  background: var(--card);
  padding: 1rem 1.5rem;
  border-radius: 8px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.3);
  display: flex; justify-content: space-between; align-items: center;
}
.dictionary-item h2 { color: var(--accent); }

Footer

Simple centered footer

Contains copyright + social links

Class: <footer>

Key styles:

footer { background: var(--card); text-align: center; padding: 2rem 1rem; font-size: 0.9rem; color: #777; }

Theme Toggle Script

Immediately-invoked function expression (IIFE)

Manages toggling light, dark, system

Updates icon and saves preference in localStorage

Connected to .hamburger button

Other Base Styles

* { box-sizing: border-box; margin: 0; padding: 0; }

body { font-family: ..., background: var(--bg); color: var(--text); padding-top: 120px; }

a { color: var(--accent); text-decoration: none; }

✅ Summary Table

Component	Class / Tag	Key Features / Styles
Navbar	.navbar	Fixed top, logo + links, theme toggle button
Hero Section	.hero	Gradient background, centered h1 + p
Dictionary List	.dictionary-list	Container for .dictionary-item
Dictionary Item	.dictionary-item	Card style, flex layout, accent heading
Footer	footer	Centered text, social links, small font
Theme Variables	:root	--bg, --card, --text, --accent, --accent-hover
Theme Script	#theme-toggle	Toggle light/dark/system, localStorage persistence

